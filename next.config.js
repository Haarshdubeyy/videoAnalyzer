/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.ytimg.com',      // YouTube thumbnail domain
      'img.youtube.com',  // YouTube default thumbnail domain
    ],
  },
}

module.exports = nextConfig 